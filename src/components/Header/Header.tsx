import { Logo } from "..";

export const Header = () => {
  return (
    <header className="flex py-5 justify-center items-center bg-gray-700 border-b border-gray-600">
      <Logo />
    </header>
  );
};
